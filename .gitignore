node_modules/
dist/
.env
.env.local
.env.*.local

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Claude Code local files
.claude/
check_app.mjs

.vercel

# Supabase CLI local project link cache
supabase/.temp/

# Output di build Android/iOS (Capacitor) — mai da committare, generati localmente
*.apk
*.aab
*.ipa
android/app/build/
android/.gradle/
android/local.properties

# Keystore di firma release Android — MAI committare (password + chiave privata)
android/keystore.properties
android/keystore/
ios/App/Pods/
ios/App/build/
ios/App/DerivedData/
